[project]
name = "discharge_app"
version = "1.0.0"
description = "入河排污口现场记录工具"
requires-python = ">=3.8"
dependencies = [
    "flet"
]

[tool.flet]
org = "com.yourname"
product = "discharge_app"   # 改为英文
